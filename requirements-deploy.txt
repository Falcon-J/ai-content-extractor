# Deployment-specific requirements for Streamlit Cloud
# This file contains only the essential dependencies needed for deployment

streamlit>=1.28.0
beautifulsoup4>=4.12.0
html5lib>=1.1
python-dotenv>=1.0.0
validators>=0.22.0
pandas>=2.1.0
openpyxl>=3.1.0
requests>=2.31.0

# Note: langchain and selenium dependencies are excluded for deployment
# The app automatically switches to deployment-friendly versions
